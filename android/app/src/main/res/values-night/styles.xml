<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is on.
         Same navy/white splash treatment regardless of system dark mode — see values/styles.xml for why
         Theme.SplashScreen (androidx.core:core-splashscreen) is used instead of a plain windowBackground drawable. -->
    <style name="LaunchTheme" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenBackground">@color/splashBg</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/ic_launcher_background</item>
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
    </style>
    <!-- Normal theme applied once Flutter is running. Must mirror values/styles.xml
         exactly — a Material parent + transparent background is what makes
         enableEdgeToEdge() (MainActivity.kt) compute correct system-bar
         contrast; the old framework Theme.Black.NoTitleBar this used to point
         at doesn't declare the attributes enableEdgeToEdge() reads, and it
         fell back to an opaque black status-bar scrim. -->
    <style name="NormalTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
