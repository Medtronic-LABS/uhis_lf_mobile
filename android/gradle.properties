org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Without this, the Flutter Gradle plugin unconditionally resets
# defaultConfig.ndk.abiFilters to its own full platform list after
# evaluation, silently overriding the arm64-v8a/armeabi-v7a-only filter set
# in app/build.gradle.kts. See flutter_tools/gradle FlutterPluginUtils.kt
# (PROP_DISABLE_ABI_FILTERING) — this is the documented opt-out.
disable-abi-filtering=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
