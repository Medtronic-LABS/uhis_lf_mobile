name: uhis_next
description: "UHIS Next — MedtronicLabs frontline health worker app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  dio_cookie_manager: ^3.1.1
  cookie_jar: ^4.0.8
  flutter_secure_storage: ^9.2.2
  go_router: ^14.6.1
  provider: ^6.1.2
  crypto: ^3.0.6
  local_auth: ^2.3.0
  local_auth_android: ^1.0.46

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
